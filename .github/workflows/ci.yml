name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  backend-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: timescale/timescaledb:latest-pg15
        env:
          POSTGRES_DB: pcafe2025_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.22"

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('backend/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Install dependencies
        working-directory: ./backend
        run: go mod download

      - name: Run tests
        working-directory: ./backend
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: pcafe2025_test
          DB_USER: postgres
          DB_PASSWORD: password
          ENV: test
        run: go test -v ./...

      - name: Build
        working-directory: ./backend
        run: go build -o bin/server main.go

  frontend-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.bun/install/cache
            frontend/node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('frontend/package.json', 'frontend/bun.lock*') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        working-directory: ./frontend
        run: bun install

      - name: Type check
        working-directory: ./frontend
        run: bun run typecheck

      - name: Lint
        working-directory: ./frontend
        run: bun run lint

      - name: Build
        working-directory: ./frontend
        run: bun run build

  # playwright-e2e:    
  #   runs-on: ubuntu-latest

  #   services:
  #     postgres:
  #       image: timescale/timescaledb:latest-pg15
  #       env:
  #         POSTGRES_DB: pcafe2025_test
  #         POSTGRES_USER: postgres
  #         POSTGRES_PASSWORD: password
  #       options: >-
  #         --health-cmd pg_isready
  #         --health-interval 10s
  #         --health-timeout 5s
  #         --health-retries 5
  #       ports:
  #         - 5432:5432

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Set up Go
  #       uses: actions/setup-go@v4
  #       with:
  #         go-version: "1.22"

  #     - name: Setup Bun
  #       uses: oven-sh/setup-bun@v1
  #       with:
  #         bun-version: latest

  #     - name: Cache Go modules
  #       uses: actions/cache@v3
  #       with:
  #         path: |
  #           ~/.cache/go-build
  #           ~/go/pkg/mod
  #         key: ${{ runner.os }}-go-${{ hashFiles('backend/go.sum') }}
  #         restore-keys: |
  #           ${{ runner.os }}-go-

  #     - name: Cache frontend dependencies
  #       uses: actions/cache@v3
  #       with:
  #         path: |
  #           ~/.bun/install/cache
  #           frontend/node_modules
  #         key: ${{ runner.os }}-bun-${{ hashFiles('frontend/package.json', 'frontend/bun.lock*') }}
  #         restore-keys: |
  #           ${{ runner.os }}-bun-

  #     - name: Install backend dependencies
  #       working-directory: ./backend
  #       run: go mod download

  #     - name: Install frontend dependencies
  #       working-directory: ./frontend
  #       run: bun install

  #     - name: Build backend
  #       working-directory: ./backend
  #       run: go build -o bin/server main.go

  #     - name: Build frontend
  #       working-directory: ./frontend
  #       run: bun run build

  #     - name: Run database migrations
  #       working-directory: ./backend
  #       env:
  #         DB_HOST: localhost
  #         DB_PORT: 5432
  #         DB_NAME: pcafe2025_test
  #         DB_USER: postgres
  #         DB_PASSWORD: password
  #         ENV: test
  #       run: ./bin/server migrate

  #     - name: Start backend server
  #       working-directory: ./backend
  #       env:
  #         DB_HOST: localhost
  #         DB_PORT: 5432
  #         DB_NAME: pcafe2025_test
  #         DB_USER: postgres
  #         DB_PASSWORD: password
  #         ENV: test
  #         SERVER_PORT: 8080
  #         SERVER_HOST: 0.0.0.0
  #       run: |
  #         echo "Starting backend server..."
  #         ./bin/server > server.log 2>&1 &
  #         SERVER_PID=$!
  #         echo "Backend server started with PID: $SERVER_PID"
  #         echo $SERVER_PID > server.pid

  #     - name: Wait for backend to be ready
  #       run: |
  #         echo "Waiting for backend to be ready..."
  #         timeout 60 bash -c 'until curl -f http://localhost:8080/health; do 
  #           echo "Backend not ready yet, waiting..."
  #           sleep 2
  #         done'
  #         echo "Backend is ready!"
          
  #     - name: Show server logs if startup failed
  #       if: failure()
  #       working-directory: ./backend
  #       run: |
  #         echo "=== Backend Server Logs ==="
  #         cat server.log || echo "No server logs found"
  #         echo "=== End Server Logs ==="
          
  #     - name: Verify backend endpoints
  #       run: |
  #         echo "Testing backend endpoints..."
  #         curl -f http://localhost:8080/health
  #         curl -f http://localhost:8080/debug/routes

  #     - name: Install Playwright Browsers
  #       working-directory: ./frontend
  #       run: bunx playwright install --with-deps

  #     - name: Run Playwright tests
  #       working-directory: ./frontend
  #       env:
  #         PLAYWRIGHT_BASE_URL: http://localhost:8080
  #       run: bun run test:e2e

  #     - name: Show server logs if Playwright tests failed
  #       if: failure()
  #       working-directory: ./backend
  #       run: |
  #         echo "=== Backend Server Logs During Tests ==="
  #         cat server.log || echo "No server logs found"
  #         echo "=== End Server Logs ==="
  #         echo "=== Server Process Status ==="
  #         if [ -f server.pid ]; then
  #           SERVER_PID=$(cat server.pid)
  #           ps aux | grep $SERVER_PID || echo "Server process not found"
  #         fi

  #     - name: Cleanup backend server
  #       if: always()
  #       working-directory: ./backend
  #       run: |
  #         if [ -f server.pid ]; then
  #           SERVER_PID=$(cat server.pid)
  #           kill $SERVER_PID || echo "Server already stopped"
  #           rm server.pid
  #         fi

  #     - name: Upload Playwright Report
  #       uses: actions/upload-artifact@v4
  #       if: always()
  #       with:
  #         name: playwright-report
  #         path: frontend/playwright-report/
  #         retention-days: 30

  markdown-lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install markdownlint
        run: npm install -g markdownlint-cli

      - name: Lint markdown files
        run: markdownlint **/*.md --ignore node_modules --ignore '**/node_modules/**'
